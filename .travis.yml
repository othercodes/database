language: php

php:
  - '5.3'
  - '5.4'
  - '5.5'
  - '5.6'
  - '7.0'

dist: trusty

addons:
  apt:
    packages:
      - mysql-server
      - mysql-client
    services:
      - mysql

before_script:
  - if [[ $TRAVIS_PHP_VERSION = hhvm ]]; then mysql -u root -e 'create database test;'; fi
  - if [[ $TRAVIS_PHP_VERSION = hhvm ]]; then mysql -u root test < examples/example-database.sql; fi

script:
  - phpunit -c ./tests/phpunit.xml